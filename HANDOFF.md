# HANDOFF — передача состояния

Читаешь это в новом окне и не помнишь ни одного разговора. Здесь **состояние,
живые данные и история решений**; как устроен код — в [CLAUDE.md](CLAUDE.md),
не дублирую.

Владелец — фрилансер, ведёт заказы сам: и переговоры, и техчасть. Он же
единственный пользователь. Всё написано для него.

---

## 1. Где мы сейчас

Работают и покрыты тестами (**297 зелёных**, один пропущен на Windows,
миграций 11): приём сообщений, роли, сбор ТЗ, планирование,
приёмка с разделением доказательств,
мультиаккаунтность, синк общей таблицы, вызовы модели через API или подписку.
Добавилось: судья отвечает на заявленный критерий, оборванный прогон убирает
за собой, один проект пишет один процесс.

### Что НЕ доделано — по важности

1. **Мультиаккаунтность не проверена живьём.** Два бот-токена в vault есть
   (`QORSA_BOT_TOKEN`, `HUSTLE_BOT_TOKEN`, по 46 символов), но ingest
   с двумя подключениями одновременно ни разу не запускался. Изоляция
   отправки покрыта только тестами.

2. **MAX не проверен ничем.** Токена нет и взять негде: MAX пускает на
   платформу только верифицированные юрлица РФ — внешняя блокировка на
   стороне владельца. Компания `maxru` в `accounts.toml` стоит
   `active=false`. Открытый вопрос, на который нет ответа в документации:
   нужны ли боту права администратора в группе MAX, чтобы видеть все
   сообщения (аналога `/setprivacy` в описании нет).

3. **Первая запись в рабочую таблицу не делалась.** Синк гонялся только
   на чтение.

4. **Потолок расхода подписки задан на глаз.** `DAILY_CLI_BUDGET_USD=25` —
   число из головы, а не из наблюдений: сравнивать не с чем, пятичасовое окно
   снаружи не видно, и меряем мы оценку, которую отдаёт сам CLI. Один
   ориентир есть: день работы с брифом и планом по проекту 8 дал ~$6 при
   14 вызовах и 107 минутах. Когда автопилот начнёт крутиться сам, цифру
   надо будет поправить по факту — сейчас она может как встать поперёк
   работы, так и не удержать ничего.

---

## 2. Рабочая таблица — реальная, с живыми заказами

```
SHEET_ID  1zZF9PkTd3jh5vEIj9kGLN-FzT6_6--P-w2Jgci0ZlGI
лист      Задачи
заголовки ПЕРВАЯ строка (у старой таблицы они были во второй, отсюда переезд)
доступ    service_account.json в корне, права writer выданы и проверены
```

Одна таблица на все три компании, строку привязывает колонка «Компания»
(значения `qorsa` | `hustle` | `max`, выпадающий список).

**21 колонка, слева направо:** Лист CRM · Строка CRM · Дата · Заказ · Клиент ·
Остаток клиента · Описание · Приоритет · Статус · Дата выполнения ·
**Стадия заказа** · Комментарий · **Нужно от клиента** · Срок · **ID** ·
Компания · Чат клиента · Готов к работе · Папка · **Прогресс** · **Превью**

Жирным — колонки бота, остальные его не касаются.

### Переиспользованные колонки — здесь легко затереть чужое

| колонка | чья | смысл |
|---|---|---|
| **Стадия заказа** | БОТА | его собственный статус (`STATUS_RU`) |
| Статус | ТВОЯ | Не начато / В работе / Выполнено частично / Выполнено / Возврат |
| **Нужно от клиента** | БОТА | чеклист недостающих доступов |
| Срок | твоя, бот **читает** | дедлайн проекта, питает буст WFQ |
| Дата выполнения | ТВОЯ | бот не трогает |
| **ID** | БОТА | раньше был человеческой колонкой, теперь честно его |

Тест `test_sheets_column_ownership` проверяет обе стороны: что бот пишет
в «Стадия заказа» и что индекс колонки «Статус» не появляется среди записей.

### Прочитано с живой таблицы (только чтение, ничего не записано)

```
строк на листе      499
пустых пропущено    461
разобрано заказов    38   ->  qorsa 21, hustle 7, max 10
строк под вопросом    0
```

**«Чат клиента» пуст у ВСЕХ 38 строк — и это блокирует работу целиком.**
Проект уходит из `new` в `briefing` только когда чат заполнен; сейчас таких
ноль, то есть боту некуда писать ни одному клиенту. Хендлы в таблице есть,
но лежат внутри колонки «Клиент» свободным текстом вперемешку с телефонами
и описаниями («@dimitrii_mati», «Анастасия в ТГ на хасле», «8915 754 97 86»).
Разбирать их оттуда я не стал: любая догадка кончится письмом не туда.

Заодно пусты «Срок» (буст WFQ не сработает ни разу) и «Папка». «Приоритет»
заполнен у 7 из 38. «Готов к работе» приходит СТРОКОЙ `'FALSE'`, а не bool,
и стоит у всех — то есть третий гейт build закрыт для всего.

**`SHEET_REQUIRE_CHAT=1` по умолчанию** именно поэтому: без него первая же
запись проставила бы `ID` во все 38 строк разом. Проверено — сейчас запись
не тронет ни одной ячейки.

Колонки «Стоимость $» в листе НЕТ, а в `BOT` она есть. Заводить — 22-й,
после «Превью» (порядок коду безразличен, ищется по названию).

---

## 3. Живые данные: проект 8

**Единственная настоящая переписка в базе.** Всё остальное — синтетика,
которую я написал сам (проект 2) или демо (`seed_demo.py`, проекты 3–7).

```
проект 8, компания hustle, «Разработка приложения доставки»
клиент Анастасия, чат telegram:809281993, 132 сообщения
```

Импорт JSON-экспорта Telegram: `--project 8 --owner-id 8649452219`.
Роли легли `client=55 / owner=77` — сошлось с `from_id` в файле.
Вложений 12 (8 фото, 4 документа), секрет перехвачен один
(`P8_LABELLED_LINE_1`, помеченная строка в сообщении владельца).
Доступов клиента в переписке нет вообще — проверял счётчиками, ноль
вхождений «пароль», «логин», «доступ», почт и ключей.

Проект — приложение доставки **TUR TUK** в отели Кемера (Турция) для
русскоязычных туристов: клиентское приложение, курьерское, админ-панель,
оплата картами РФ и СБП, три языка, публикация в сторах.

### Бриф ВЫВЕРЕН по исходному сообщению клиента

Требование владельца: ТЗ обязано строго соответствовать сказанному клиентом —
ни потерь, ни добавлений. Источник правды — сообщение **6742**: заполненный
клиентом бриф на 3851 символ, ответы на 17 разделов.

Разобрано по разделам и сверено с тремя источниками (прогон на 25 пунктов,
накопленные 38 из `backup/brief8_before_parsefix.json`, само сообщение).
**Потеряно было шесть вещей**: повтор заказа, единые цены вне зависимости от
отеля, дата рождения и имя при регистрации, складская система «позже»,
маркетплейс «в дальнейшем», один магазин на старте. Ещё два пункта стояли
не на той стороне — статистика и интервалы доставки лежали в работе как
`nice`, хотя клиент их прямо отложил.

В накопленных 38 нашлись две натяжки: «Роль Промоутер» как требование (клиент
написал «Промоутеры**?**» — это вопрос) и вопрос про тип чат-бота (его задала
модель, а не клиент). Обе не возвращены. Остальная разница между 38 и 25 —
не потери, а дубли: курьерское приложение там перечислено трижды, чат
с оператором дважды, загрузка товаров владельцем трижды.

Каждый восстановленный пункт помечен `curated` и **прошёл ту же
`Brief.apply_evidence`, что и ответ модели** — то есть держится на словах
клиента, а не на трактовке. Итог: 26 требований, 6 ограничений,
9 в `out_of_scope`, 2 блокирующих вопроса (расчёт стоимости доставки и выбор
платёжной системы), confidence 0.80. Контрольный список из 21 требования
и 9 исключений сходится полностью.

Что важно понимать про механику: **`brief_eval` полным пересбором эту правку
снесёт** — он чистит бриф перед прогоном. Инкрементальные проходы её
сохранят (пункт, не подтверждённый прогоном, помечается `missing`, но
остаётся). Снимок выверенного состояния стоит держать в `backup/`.

### История брифа: как он до этого дошёл

```
сейчас в базе   26 пунктов, confidence 0.80   <- выверено по 6742,
                                                 снимок в backup/brief8_curated.json
до правки       25 пунктов, confidence 0.80   <- один чистый прогон CLI
до этого        38 пунктов, confidence 0.75   <- сложение API и CLI,
                                                 снимок в backup/brief8_before_parsefix.json
чистый API      32 пункта                     <- снимок в backup/brief8_api.json
```

`brief_eval` чистит бриф перед полным пересбором, поэтому прогон 16 августа
заменил смесь одним связным результатом. Накопление при этом никуда не делось
и снова работает — просто счёт пошёл с нуля.

Разброс между прогонами по-прежнему велик: 25 пунктов против 31 у прошлого
CLI-прогона и 32 у API. Совпадение API и CLI когда-то было 23 из 32 (**72%**)
— столько же, сколько между двумя прогонами API на одном чате. Это свойство
модели, а не поломка; лечится только несколькими прогонами (`BRIEF_SAMPLES`),
то есть кратным расходом окна.

**Срок и бюджет сходятся во всех прогонах, на обоих бэкендах:**

```
срок        2026-09-01
бюджет      150 000 ₽
confidence  0.80 сейчас (было 0.75 впритык к порогу на прошлых прогонах)
```

Это и есть то, ради чего дедлайн с бюджетом вынимаются из переписки: план
опирается на них напрямую, а гуляют они меньше всего остального.

### Запрет на подмену стека сработал ВЖИВУЮ — и упёрся в решение владельца

Прогон с зафиксированным стеком провалился дважды подряд, и оба раза по делу:

```
попытка 1 (24.7 мин): + React + Vite + Ant Design — веб-админка
                      + WebSocket (FastAPI native)
                      + Docker Compose — деплой
попытка 2 (48.1 мин): + Jinja2/Starlette — веб-админка «встроена в FastAPI,
                        нет отдельного фронтенд-фреймворка»
                      + WebSocket «встроен в FastAPI/Starlette»
```

Механизм отработал как задумано: подмена не принята молча, модель получила
повтор с текстом «стек ЗАФИКСИРОВАН владельцем», при упорстве прогон
провалился, проект ушёл в `blocked` с внятной причиной. План не тронут.

**Но вторая попытка показала, что запрет надо уточнить.** Модель не подменяла
решение — она называла то, что УЖЕ ВНУТРИ зафиксированного: шаблоны Starlette
и WebSocket входят в FastAPI. В списке владельца сказано «Админка: веб,
отдельно от мобильных приложений», но не сказано, на чём она; realtime для
чата и трекинга курьера не назван вовсе. Планировать, не назвав это, модель
не может, а назвав — нарушает запрет.

**Развилка, и решать её владельцу:**

* **дополнить список** двумя строками (веб-админка на шаблонах Starlette
  в составе FastAPI; realtime — WebSocket там же) — запрет остаётся жёстким,
  каким и заказан;
* **смягчить правило**: запрещать ЗАМЕНУ (React Native вместо Flutter,
  Firebase вместо PostgreSQL), но разрешать ДОПОЛНЕНИЕ, показывая его блоком
  «ЧАСТИЧНО ИЗ ТЗ». Тогда планировщик сам достраивает то, что владелец
  не назвал, а владелец это видит и правит.

Цена вопроса: каждая попытка плана — 25–50 минут и $1.5–3 квоты подписки.
За сутки на планы ушло $15.97 из потолка в $25.

### Архитектура ЗАФИКСИРОВАНА владельцем

Три прогона подряд по одному ТЗ дали три разные архитектуры. Выбор снят
с модели и сделан человеком — один раз:

```
Мобильные приложения: Flutter — клиентское и курьерское
Бэкенд: FastAPI + PostgreSQL, монолит
Хранилище файлов: S3-совместимое
Push-уведомления: FCM
Платежи: ЮKassa (СБП + карты РФ)
Карты: 2GIS или OpenStreetMap
Админка: веб, отдельно от мобильных приложений
```

Обоснование лежит в `workspaces/p8/docs/DECISIONS.md`: 16 дней, 150 000 ₽,
~50 заказов в день, один город. Firebase отброшен — нестабильно работает
в России, а клиенты платят русскими картами. Микросервисы не помещаются
в срок. Flutter вместо React Native — одна кодовая база на два приложения.

В брифе это семь `curated`-пунктов с `origin=owner`. Планировщику менять их
запрещено: подстановка своего — ошибка прогона, а не тихая замена. Всего
решений владельца в брифе сейчас 15 (7 стек, 3 требования, 1 ограничение,
4 в out_of_scope), и все они переживают полный пересбор.

### История: третий план, третья архитектура — почему и пришлось фиксировать

```
              задач   автономность (задачи / время)   стек
API-прогон      33          30% / 33%                React Native
CLI-прогон 1    26          65% / 54%                Flutter + Firebase
CLI-прогон 2    21          57% / 32%                Flutter + Node + PostgreSQL
```

Оба CLI-плана лежат целиком в `backup/plan8_firebase.log` и
`backup/plan8_node.log` — сравнивать есть с чем.

**Сопоставление задач на этом прогоне почти не сработало, и это правильно.**
Из 21 задачи старые строки достались двум («Публикация в App Store» и
«в Google Play» — названия совпали дословно). Остальные новые, а 24 прежние
ушли в сироты. Причина не в порогах: модель сменила архитектуру, и работа
стала другой — «Схема данных Firestore» и «Инициализация backend-монолита»
это не переформулировка, это разные задачи. Механизм спасает от
переформулировки, от перепроектирования не спасает ничто.

Сироты убраны `prune` (24 удалено, осталась одна — задача 394 с расходом).
**Вывод для будущего: после каждой смены стека будет уборка**, и делать её
надо руками, иначе не увидишь, что план переехал.

**Стек снова выбрала модель — и объявила его взятым из ТЗ.** В брифе стоит
одна строка «Мобильные приложения для iOS и Android (тип не уточнён)», а план
пришёл с пометкой «ВЗЯТ ИЗ ТЗ — планировщик его не выбирал» на
`Node.js + Express + TypeScript + PostgreSQL + Prisma + Socket.IO`. Пометку
ставил наш собственный код: есть в брифе хоть строчка про стек — значит всё
решение клиентское. Починено (`stack_not_in_brief`), непрошеное теперь
показывается блоком «ЧАСТИЧНО ИЗ ТЗ». Прогон, на котором это поймано, уже
записан в базу со старым поведением — при следующем планировании метка
станет честной.

### История: первый план на CLI — лучше того, что дал API

```
              задач   автономность (задачи / время)   вердикт
API-прогон      33          30% / 33%                малопригоден
CLI-прогон      26          65% / 54%                ГОДИТСЯ
```

Первый плановый прогон, взявший порог 50%. Разница не в бэкенде как таковом:
CLI выбрал **Flutter + Firebase** (один язык на три приложения, BaaS вместо
своего бэкенда), обосновав это прямо сроком в 16 дней и бюджетом в 150 тысяч,
и отбросил Node+PostgreSQL на VPS как «~40 часов DevOps не влезают». Работы,
которую агент может сделать и проверить сам, при таком стеке просто больше.

Прочее по прогону: подозрительных критериев **0**, задач, выведенных из цели
вместо пункта ТЗ, **0**, правок кода поверх ответа модели 9 (пять понижений
`claude_code → manual`, две задачи выброшены — `deliverable_ref` не нашёлся
в ТЗ). Решение по стеку записано в `workspaces/p8/docs/DECISIONS.md`.

Цена: две попытки, ~40 минут, оценка расхода подписки за сутки ~$5.75 при
14 вызовах и 107 минутах. **Первая попытка сгорела впустую** — см. раздел 4
про выбор JSON-объекта из прозы.

**База почищена: было 110 задач проекта 8 при 84 сиротах, стало 27 при
одной.** Сироты были не свойством перепланирования, а его поломкой:
сопоставление шло по точному названию, модель формулирует иначе, и каждый
прогон осиротял предыдущий план целиком. Починено (`match_task`), накопленное
убрано `tasks_cli prune --project 8 --apply`.

Единственная оставшаяся сирота — задача 394, на которой проверялся судья:
у неё есть строка расхода, а такие уборка не трогает намеренно. Живых
задач 26.

Служебных задач полосы `chat` («сбор ТЗ», «планирование») у проекта 35 —
столько накопилось, пока каждый вызов модели заводил новую. Теперь якорь
один на проект и название (`db.service_task`), но **старые 35 остались**:
к ним привязаны строки `Run`, и удалять их значит потерять владельца расхода.
В глаза они не лезут — в списки ручных задач полоса `chat` не попадает.

Суммарная оценка нового плана — 5250 минут, то есть **87 часов** против
16 дней до срока (у API-плана было 106). Арифметически помещается, но
оценкам верить нельзя: два мобильных приложения, бэкенд, админка, эквайринг
и публикация в двух сторах за 87 часов — не бывает, и то, что цифра стала
меньше, доверия ей не прибавляет.
Резать в первую очередь: курьерское приложение, английский и турецкий,
чат с оператором, промокоды, статистику — клиент сам говорил про них
«можно позже».

---

## 4. Что показали ЖИВЫЕ ДАННЫЕ, чего не показывали тесты

Самый ценный раздел. Каждый пункт стоил отдельного разбирательства, и ни
один не был виден на синтетике.

**Обрезка контекста считала СООБЩЕНИЯ, а не объём.** Последние 80 реплик
целиком, остальные обрубком в 120 символов. Заполненный клиентом бриф на
3851 символ оказался 53-м с конца и дошёл до модели как 120 знаков. Вся
переписка весила 15 тысяч символов и влезала целиком. Цена измерена:
**4 пункта ТЗ вместо 33**. Теперь бюджет в токенах, а длина сообщения
считается признаком содержательности: под нож идут короткие и старые.

**`text_truncated` в `unreadable[]` был сигналом модели, что её обделили
данными, — и мы его игнорировали.** Модель честно перечисляла обрезанные
сообщения, а код принимал бриф как есть. Теперь это жёсткая ошибка прогона:
CRITICAL, уведомление владельцу, бриф не принимается. Отличать от
нечитаемого вложения обязательно: «не вижу, что на фото» — свойство данных,
«текст обрезан» — наш промах.

**`.CMD` не запускается через `create_subprocess_exec`.** npm на Windows
ставит `claude.CMD`, CreateProcess обёртки исполнять не умеет — падало
с «не удаётся найти указанный файл», хотя файл на месте. Рядом лежит
настоящий `claude.exe`, `resolve_cli()` берёт его.

**Промпт не влезал в командную строку.** Через `cmd.exe` предел 8191
символ, промпт брифа — пятнадцать тысяч. Возвращалась обрубленная
абракадабра с покалеченной кириллицей вместо внятной ошибки. Промпт теперь
уходит в stdin.

**`--disallowedTools` раздувает контекст вчетверо.** Перечисление
инструментов по именам подтягивает их описания: 26k токенов кэша против
6.7k без него. Запрещать поимённо то, что и так не разрешено пустым
allowlist, — значит платить за список запретов. Замерено по отдельности,
вместе с заменой системного промпта и отключением динамических секций
(46.6k → 6.8k).

**CLI по умолчанию берёт Opus.** Один вызов из трёх слов стоил владельцу
$0.126. `--model sonnet` передаётся ВСЕГДА и явно; `Reply.models` показывает,
какая модель ответила НА САМОМ ДЕЛЕ. Проверено: резолвится в
`claude-sonnet-5`. Вторая модель в ответе — `haiku-4-5`, это внутренний
вызов самого Claude Code, не наш.

**CLI медленнее API в 3.5 раза.** Замер на девяти вызовах брифа против
девяти API: **245 секунд против 70**. API стабилен (66–73), CLI гуляет
(188–316). Сбор брифа в три прогона: 3.5 минуты на API против 12 на CLI.
Отсюда и `BRIEF_SAMPLES=1` по умолчанию.

**Судья съезжал с критерия на общее ревью — почин��но и проверено вживую.**
Промпт начинался словами «исходи из того, что дефекты есть», и модель
исходила: девять дефектов про middleware, идемпотентность, try/catch и тесты
при выполненном критерии. Теперь судью спрашивают об одном, каждый дефект
обязан цитировать нарушенную часть критерия, а всё постороннее уходит
в `observations`. На том же дифе живой CLI даёт PASS и пять замечаний.

**Ссылки на критерий мало — нужен ещё и сам дефект по теме.** Первая версия
привязки проверяла только, что выдержка правда из критерия. На первом же
тесте это пропустило «нет middleware для raw body» с выдержкой «обработчика
вебхука»: выдержка настоящая, но это ОБЪЕКТ проверки, а не требование, и под
него подводится что угодно. Пришлось добавить второй рубеж — дефект обязан
задевать словарь критерия.

**Детектор упора в квоту обыскивал ОТВЕТ МОДЕЛИ.** Самая дорогая находка
этого захода. `looks_like_limit` шёл первым и просматривал весь stdout,
а в stdout лежит результат. План приложения доставки честно расписал
«rate limit» для API — и прогон объявили упором в квоту при
`is_error:false`, `stop_reason:end_turn` и ста тысячах токенов выхода.
Результат выброшен целиком, двадцать шесть минут работы модели в мусор,
а по правилам `limits.py` в боевом режиме встала бы ВСЯ работа разом
и владельцу ушло бы уведомление о несуществующем упоре. Теперь ответ
сначала разбирается, и признаки упора ищутся только в неудачном вызове.

**Из прозы вынимался ПЕРВЫЙ объект, а не нужный.** Модель начала план
объяснением, внутри которого был свой маленький JSON — его и взяли.
Валидация честно сказала «tasks должен быть непустым списком», прогон ушёл
на вторую попытку, а настоящий план лежал ниже в том же ответе. Тринадцать
минут работы модели впустую. Теперь `parse_judge_json(raw, require=...)`
опознаёт объект по ключу: `tasks` у плана, `deliverables` у брифа, `verdict`
у судьи.

**Живой прогон брифа с этой правкой прошёл насквозь** (25 пунктов,
confidence 0.80) — значит `require` не ломает нормальный случай. Но саму
починку он НЕ проверил: модель на этот раз отдала чистый JSON, строки
«начат прозой» в логе нет. Ветка с прозой и объектом-обманкой вживую
не воспроизводится по заказу — она закрыта только тестом.

**Настоящий упор в квоту приняли за поломку модели.** Живой текст CLI:
`You've hit your session limit · resets 5:50pm (Asia/Qyzylorda)`. Ни один
маркер не подошёл — в списке было «usage limit», а пришло «session limit»;
было «resets at», а пришло «resets 5:50pm». Прогон объявили недоступностью
модели (`LLMError`), то есть поломкой. **В бою это стоило бы задаче
засчитанной попытки и дороги в эскалацию** — ровно то, против чего написан
весь `limits.py`. Маркеры расширены, `retry_after_from` научился читать
время сброса, а не только «через сколько». Расширять их стало безопасно
потому, что успешный ответ мы больше не обыскиваем.

Урок повторный и общий: **список формулировок устаревает молча**. Смотреть
надо не на то, что список есть, а на то, ловит ли он последний живой текст.

**План на CLI перестал укладываться в получас.** Два прогона подряд по
выверенному брифу (26 пунктов) упёрлись в `CLI_TIMEOUT_SEC=1800`: `Run` 68 —
ровно 30.0 минут, машина при этом не спала. Разброс огромен: удачный прогон
того же плана занял 11 минут, а первая попытка в тот день — 26. Причина
та же, что и с сотней тысяч токенов: длину ответа CLI ничем не ограничивает,
и сколько модель будет писать — как повезёт. Третий прогон пущен
с `CLI_TIMEOUT_SEC=3600`.

**Оборванный вызов жжёт квоту, а в учёте даёт ноль.** Стоимость берётся из
ответа CLI, а у срубленного таймаутом ответа нет: строка закрывается
`ok=0, cost_usd=0`. Два получасовых таймаута сожгли реальную квоту и не
сдвинули `DAILY_CLI_BUDGET_USD` ни на цент. Потолок считает по нижней
границе — на прогонах, которые часто обрываются, он не удержит.

**«CLI не принимает max_tokens» — я записал это неверно, и ошибка стоила
трёх прогонов.** Потолок у CLI есть, просто задаётся не флагом, а переменной
окружения `CLAUDE_CODE_MAX_OUTPUT_TOKENS`. Вскрылось живым ответом:
«Claude's response exceeded the 32000 output token maximum. To configure
this behavior, set the CLAUDE_CODE_MAX_OUTPUT_TOKENS environment variable».
До этого `BRIEF_MAX_TOKENS` и `PLAN_MAX_TOKENS` на подписке молча не значили
ничего — отсюда и 100 668 токенов выхода за 26 минут на одном из прогонов.
Теперь значение уходит дочернему процессу, а потолки разведены по бэкендам:
`PLAN_MAX_TOKENS` (API) и `PLAN_MAX_TOKENS_CLI` (подписка, 48000).

**`self.uses_api` не существовало вовсе.** Свойство упоминалось в двух местах
`verifier.py` и нигде не определялось — фаза 9 его потеряла при переводе
судьи на подписку. Каждая `llm`- и `screenshot`-проверка падала с
`AttributeError`, а код добросовестно записывал это в дефекты задачи: приёмка
проваливалась ещё до обращения к модели. Ни один тест этого не показал,
потому что судью в тестах поднимали с готовым бэкендом.

**Откушенный последний символ у пароля в vault.** Ловилось раньше, поэтому
теперь целостность проверяется программно: значение подставляется обратно
в текст сообщения и сверяется с исходником байт в байт. На проекте 8
проверено — совпало.

**Составной `deliverable_ref` рушил граф зависимостей.** Модель склеивает
несколько пунктов в одну ссылку («Клиентское приложение (must); Админ-панель
(must)»), целиком такая строка не совпадала ни с чем. Пять задач выброшены,
включая инициализацию репозитория и E2E-тестирование, следом код снял
зависимости на несуществующие задачи — граф разъехался у шести штук.
`split_ref()` разбирает составную ссылку.

**Дедлайн и бюджет вырезались постфильтром.** Клиент прямо писал «первая
версия к началу сентября, бюджет 150 тысяч», а запрет «не лезь в деньги
и сроки» выбрасывал это вместе с разговорами о цене. Следствие: планировщик,
не зная про две недели и 150 тысяч, предложил **микросервисы за API Gateway
с PostgreSQL и Redis** на приложение для одного города с полусотней заказов
в день. Теперь это справочные поля `deadline`/`budget`, и они уходят
планировщику блоком «РАМКИ ПРОЕКТА».

**Заодно тот же фильтр убил бы деньги ВНУТРИ продукта:** промокоды,
минимальную сумму заказа 3000 ₽, расчёт стоимости доставки. `is_commercial()`
теперь различает по контексту, а `open_questions` освобождены от фильтра
совсем — вопрос ничего не обещает клиенту.

**Модель не знает сегодняшнего числа.** Сначала прочитала «начало сентября»
как прошлогоднюю дату (дала бы проекту вечный четырёхкратный вес в WFQ),
потом посчитала до сентября 2026 «14 месяцев», хотя оставалось две недели.
Дата теперь идёт в оба промпта явно, остаток считает код.

---

## 5. Решения и их основания

**Класс проверяемости только ПОНИЖАЕТСЯ.** Модель честно сказала `human`
про «вёрстку по макету Figma», но прицепила `http 200` — и код поднял класс
до `auto`. План вышел «100% авто», включая финальное тестирование заказа.
Признание модели в непроверяемости весомее наличия формальной проверки.
Та же логика у `executor`.

**Бриф НАКАПЛИВАЕТ и не удаляет.** Модель недетерминирована: два прогона
по одному чату дают разный состав. Потерянный отказ вернётся сам, а
потерянное требование просто не попадёт в план, и этого никто не заметит.
Единственный автоматический путь удаления — явный отказ клиента.
Побочный эффект виден на проекте 8: бриф там смешанный (раздел 3).

**Оценка модели ≠ зелёный тест.** `screenshot` и `llm` дают вердикт, который
мы сами называем подсказкой. Задача, прошедшая только ими (или только
критериями, которые `suspicious()` считает пустышками), не закрывается сама
— уходит на `/confirm`. Иначе «готово» означало бы, что модель одобрила
скриншот.

**Нет Redis/Celery/Postgres/Docker.** Один asyncio-процесс и SQLite — это
осознанный потолок. Нагрузка — до полусотни проектов одного человека; всё
сложнее добавляет операционную работу владельцу, который и так один.
Планировщик полос и WFQ написаны под это, менять их запрещено.

**Исполнение на ПОДПИСКЕ, а не на API — потому что денег на API нет.**
`LLM_BACKEND=cli` по умолчанию. API остался рабочим, возврат — одна
переменная. Отсюда же раздельный учёт: `spent_today()` считает только
`backend="api"`, иначе суточный бюджет вставал бы на пустом месте, когда
всё идёт по подписке и не тратится ни рубля.

**Упор в квоту — НЕ провал работы.** Попытка не засчитывается, бриф не
обнуляется, встаёт вся работа сразу, владельцу сообщаем раз за инцидент.
Четыре упора подряд иначе увели бы живую задачу в эскалацию.

**Evidence подтверждает ПРОИСХОЖДЕНИЕ, а не смысл.** Код проверяет, что
сообщение существует и что оно от клиента (или это предложение владельца
плюс явное согласие). Что сообщение действительно обосновывает пункт — код
судить не может. Поэтому `brief_eval` печатает переписку рядом с брифом:
смотреть должен человек.

**Молчаливый пропуск проверки — худший режим системы**, он выглядит как
успех. Отсюда: неизвестный тип, критерий без обязательного поля и «ни одной
исполнимой проверки» — это провал, а не основание пропустить приёмку.

**Стек выбирает планировщик, но обосновывает ограничениями.** Правило по
умолчанию — самое простое решение. Решение пишется в `docs/DECISIONS.md`
проекта. Раньше здесь стоял блокирующий вопрос клиенту; владелец решил, что
останавливаться не надо, — выбирай сам и объясняй.

---

## 6. Ловушки, на которые уже наступали

**Фикстуры тихо ломаются от новых обязательных полей.** Схема брифа получила
`priority`, потом `blocking`; задача — `verify_class` и `executor`; проект —
`account_id` (NOT NULL). Каждый раз это роняло либо тесты, либо демо:
`seed_demo` создавал задачи, которые новый гейт не пускал в build, и демо
молча вставало. **Добавляешь обязательное поле — проверь `tests/conftest.py`
и `scripts/seed_demo.py`.**

**heredoc калечит экранирование.** `\n` внутри `<<'PYEOF'` превращался
в настоящий перенос строки прямо посреди f-строки, и файл переставал
парситься. Случалось четырежды за сессию. **Для правок с кавычками и
переносами используй Edit/Write, а не heredoc с python-скриптом внутри.**
Проверено ещё раз и потрачено время: замена по образцу, содержавшему `\n`,
молча не нашла ничего — образец приехал в питон уже с настоящим переносом.

**Тесты зависели от времени суток.** Тихие часы 23–9 держат сообщения
клиенту, и половина тестов про отправку была зелёной днём и красной ночью.
Сейчас `QUIET_START=QUIET_END=0` в `tests/conftest.py`.

**Плейсхолдеры из `.env.example` считались валидной настройкой.**
`ANTHROPIC_API_KEY=sk-ant-...` выглядел заданным, код уходил в API и получал
401 вместо внятного «ключа нет». Позже то же повторил `SHEET_ID=1AbC...`.
Лечится `vault._usable()` — используй его для любой новой настройки.

**`INSERT OR IGNORE` в миграции глотает не только дубли.** Он проглотил
`NOT NULL` от колонки, добавленной следующей фазой без `server_default`:
миграция «успешно» перенесла ноль строк. Правила два — булевым колонкам
давать `server_default`, а после переноса явно сверять количества.

**Таймаут оболочки убивает shell, но НЕ Python.** Оборванный на десятой
минуте `brief_eval` пережил SIGTERM, продолжил работать, дописывал строки
в `runs` и жёг квоту — а бриф остался обнулённым, потому что до обработчика
восстановления дело не дошло. Если прогон долгий, запускай в фоне и жди,
а не обрывай.

Закрыто `autopilot/guard.py`: сигнал теперь превращается в отмену внутри
корутины (восстановление отрабатывает), дочерний claude убивается вместе
с родителем, а замок на проект не пускает второй процесс — но **от
`taskkill /F` по самому питону не спасает ничто**, там уборки не будет
по определению. Дети в этом случае всё равно умрут: их держит job object.
Проверять `Get-CimInstance Win32_Process` по-прежнему полезно.

**Сон машины съедает таймаут: прогон висит часами, а не 30 минут.**
`CLI_TIMEOUT_SEC=1800` держится на `asyncio.wait_for`, а тот считает по
таймеру цикла событий — пока Windows спит, цикл заморожен вместе с дочерним
процессом, и таймер срабатывает только на пробуждении. Живой случай:
`Run` 67 — план начат в 06:28, оборван в 11:04, **276 минут** вместо 30,
`ok=0`. Ровно тот случай, который видно только благодаря строке, заводимой
ДО вызова: раньше от такого прогона в учёте не осталось бы ничего.
Прогоны дольше получаса запускай там, где машина не уснёт, либо проверяй
`runs` на строки с пустым или неправдоподобным `finished_at`.

**Вывод длинного прогона в файл не видно до самого конца.** Python буферизует
stdout, когда это не терминал: `plan_eval > log` тринадцать минут показывает
пустой файл. Живой признак работы — не размер лога, а процесс `claude.exe`
в списке и файл `locks/p8.lock` с pid.

**`brief_eval` чистит бриф ПЕРЕД прогоном.** Это by design (полный
пересбор), и при штатном провале он восстанавливает прежнее. При убийстве
процесса — нет. Снимок брифа проекта 8 лежит в `backup/brief8_api.json`.

**`Run.started_at` на самом деле время ОКОНЧАНИЯ** — строка создавалась после
вызова. Исправлено: строка заводится до вызова, у неё появился `finished_at`,
и пустой `finished_at` теперь значит «вызов не вернулся». У строк, записанных
раньше, он пуст навсегда — когда они начались, никто не записывал.

**Плавающий тест оказался живой гонкой в защите от ложной эскалации.**
`test_three_limits_do_not_grow_attempts` краснел раз из пяти. Поймал повтором
полного прогона восемь раз подряд: `ver.calls == 4` при трёх тиках. Причина
не в тесте — `_on_limit` возвращает задачу в очередь как `ready`, и **тот же
самый проход `_dispatch` подхватывает её снова**, в уже закрытое окно.
Проверки в начале `tick()` не хватает: окно закрывается посреди тика.

То есть правило «упёрлись в квоту — встала вся работа» переставало работать
ровно в тот момент, ради которого писалось, и остаток окна сгорал на пустых
попытках — столько раз, сколько свободно слотов. Теперь `limits.state.blocked()`
проверяется перед каждой выдачей слота — и **перед созданием работника тоже**:
первая версия заслона стояла только в начале прохода, между ней и `_claim`
два `await`, и упор, случившийся в это окно, всё равно пропускал лишний вызов.
Флак пережил её и вылез на четвёртом прогоне из восьми. Задача, захваченная
перед самым закрытием окна, возвращается в очередь как была (`_release`):
работать по ней не начинали.

Тест на это отдельный; после второй правки четырнадцать полных прогонов
подряд зелёные (8 + 6).

**Урок общий: плавающий тест — это сообщение о гонке в коде, а не о плохом
тесте.** Здесь он двое суток выглядел как «перезапусти и не пугайся».

**Нечёткое сопоставление нетранзитивно.** Кластеризация пунктов брифа
сравнивала кандидата только с первым элементом группы; A~B и B~C при A≁C
разваливали один пункт на два. Сравнивай со всей группой.

---

## 7. Как запускать

**Всё через `.venv`.** Системный python падает с `ModuleNotFoundError`.

```bash
# тесты (297 зелёных, один пропущен на Windows)
.venv/Scripts/python.exe -m pytest -q

# планировщик вживую, без сети и без трат
.venv/Scripts/python.exe scripts/seed_demo.py
DRY_RUN=1 QUIET_START=0 QUIET_END=0 .venv/Scripts/python.exe -m autopilot.main

# бриф и план по живому проекту 8
.venv/Scripts/python.exe scripts/brief_eval.py --list
.venv/Scripts/python.exe scripts/brief_eval.py --project 8 --samples 1
.venv/Scripts/python.exe scripts/plan_eval.py --project 8

# судья по одной задаче: на чём он ставит FAIL и что заметил попутно
.venv/Scripts/python.exe scripts/judge_eval.py --list --project 8
.venv/Scripts/python.exe scripts/judge_eval.py --task 394 --diff tests/fixtures/judge/394.diff

# ручные задачи
.venv/Scripts/python.exe scripts/tasks_cli.py list --project 8
.venv/Scripts/python.exe scripts/tasks_cli.py prune --project 8   # сироты: примерка
.venv/Scripts/python.exe scripts/tasks_cli.py done 17       # «сделал, проверь»
.venv/Scripts/python.exe scripts/tasks_cli.py confirm 17    # «закрываю под свою ответственность»

# секреты (значения не показываются, кроме get)
.venv/Scripts/python.exe scripts/vault_cli.py list
```

**Прогоны на CLI долгие** (бриф ~4 минуты на прогон, план ~13). Десятиминутный
потолок терминала их не переживёт — запускай в фоне и дожидайся, не обрывая.

### Ключи и токены

Всё в vault (`secrets.enc`, ключ шифрования `VAULT_KEY` в `.env`).
`vault.resolve_secret()` ищет в трёх местах по порядку: vault, окружение,
`.env`, и плейсхолдеры игнорирует.

```
ANTHROPIC_API_KEY   есть (денег на балансе нет — работа идёт по подписке)
QORSA_BOT_TOKEN     есть
HUSTLE_BOT_TOKEN    есть
MAXRU_MAX_TOKEN     НЕТ и взять негде — верификация юрлица РФ
```

`accounts.toml` в `.gitignore` (там id владельца), в репозитории лежит
`accounts.toml.example`.

### Claude Code CLI

Установлен и авторизован, версия 2.1.233. **Запускать надо `claude.exe`,
а не `claude.CMD`** — обёртку `create_subprocess_exec` не исполняет, а через
`cmd.exe` ломается длина командной строки и кодировка. `resolve_cli()` это
делает сам, руками лезть не надо.

```
C:\Users\user\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe
```

Для `dom` и `screenshot` нужен браузер, он установлен:
`.venv/Scripts/python.exe -m playwright install chromium`.

### Резервные копии

`backup/` (в `.gitignore`). Копию делай **консистентным снимком**, не `cp`:
рядом с базой живёт WAL, и копия одного `.db` теряет незакоммиченное.

```python
src = sqlite3.connect("autopilot.db"); dst = sqlite3.connect("backup/x.db")
with dst: src.backup(dst)
```

---

## 8. Формат работы

Каждая фаза заканчивается отчётом в чате по одному формату: **что сделал,
что нашёл сверх списка, что осталось сомнительным**. Владелец просил не
сглаживать — плохие новости про качество извлечения ему важнее зелёной
приёмки. Если цифра не измерена, так и говори, а не подгоняй.
