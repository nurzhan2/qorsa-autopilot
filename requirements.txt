sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
anthropic>=0.40
gspread>=6.1
google-auth>=2.30
httpx>=0.27
python-dotenv>=1.0
cryptography>=42
# нужен для проверок dom и screenshot; после установки:
#   python -m playwright install chromium
playwright>=1.40
# локальный дашборд (scripts/dashboard.py). Слушает только петлю;
# наружу отдавать нечего — там имена клиентов, суммы и чеклисты
fastapi>=0.110
uvicorn>=0.29

# драйвер PostgreSQL для боевой базы проектов (DATABASE_URL в vault)
asyncpg>=0.29
